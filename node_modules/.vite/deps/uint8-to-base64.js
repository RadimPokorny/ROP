import "./chunk-TDUMLE5V.js";

// node_modules/uint8-to-base64/esm/index.js
var { fromCharCode } = String;
var encode = (uint8array) => {
  const output = [];
  for (let i = 0, { length } = uint8array; i < length; i++)
    output.push(fromCharCode(uint8array[i]));
  return btoa(output.join(""));
};
var asCharCode = (c) => c.charCodeAt(0);
var decode = (chars) => Uint8Array.from(atob(chars), asCharCode);
export {
  decode,
  encode
};
//# sourceMappingURL=uint8-to-base64.js.map
